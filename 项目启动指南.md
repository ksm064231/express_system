# 快递管理系统 - 项目启动指南

## 📋 目录

- [环境要求](#环境要求)
- [项目结构](#项目结构)
- [后端启动步骤](#后端启动步骤)
- [前端启动步骤](#前端启动步骤)
- [访问测试](#访问测试)
- [常见问题排查](#常见问题排查)
- [关闭服务步骤](#关闭服务步骤)

---

## 🔧 环境要求

### 必须安装的软件

| 软件         | 版本要求                       | 验证命令          |
| ------------ | ------------------------------ | ----------------- |
| **Java JDK** | 21.x                           | `java -version`   |
| **Node.js**  | 18.x / 20.x                    | `node -v`         |
| **MySQL**    | 8.x                            | `mysql --version` |
| **Maven**    | 3.8+（项目内置 Maven Wrapper） | `mvn -v`          |

---

## 📁 项目结构

```
database_project1/
├── express_system/          # 后端（Spring Boot）
│   ├── src/
│   ├── mvnw.cmd            # Windows Maven Wrapper
│   ├── pom.xml             # Maven 依赖配置
│   └── src/main/resources/application.yml  # 数据库配置
│
└── express-frontend/       # 前端（Vue 3 + Vite）
    ├── src/
    ├── package.json        # npm 依赖配置
    └── .env.development    # 环境变量配置
```

---

## 🚀 后端启动步骤（Spring Boot）

### 步骤 1：配置数据库连接

编辑 `express_system/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/express_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root # 你的 MySQL 用户名
    password: your_password # 你的 MySQL 密码
```

### 步骤 2：创建数据库

在 MySQL 中执行（使用 Navicat 或命令行）：

```sql
CREATE DATABASE IF NOT EXISTS express_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 步骤 3：启动后端服务

#### 方式 A：使用命令行（推荐）

```bash
# 进入后端目录
cd express_system

# 设置 JAVA_HOME 环境变量（如果需要）
# Windows PowerShell:
$env:JAVA_HOME='D:\JDK'

# Windows CMD:
# set JAVA_HOME=D:\JDK

# 使用 Maven Wrapper 启动
.\mvnw.cmd spring-boot:run
```

#### 方式 B：使用 IDE（IntelliJ IDEA）

1. 用 IDEA 打开 `express_system` 文件夹
2. 等待 Maven 依赖下载完成
3. 找到 `src/main/java/com/express/ExpressApplication.java`
4. 右键点击 `main` 方法，选择 **Run 'ExpressApplication'**

### 步骤 4：验证后端启动

启动成功后，在控制台会看到：

```
Tomcat started on port 8080 (http) with context path ''
Application availability state ReadinessState changed to ACCEPTING_TRAFFIC
```

访问验证：http://localhost:8080/api/users/test

---

## 🎨 前端启动步骤（Vue 3 + Vite）

### 步骤 1：安装依赖

```bash
# 进入前端目录
cd express-frontend

# 安装 npm 依赖
npm install
```

### 步骤 2：配置 API 地址

编辑 `express-frontend/.env.development`：

```env
VITE_API_BASE_URL=http://localhost:8080/api
```

### 步骤 3：启动前端开发服务器

```bash
npm run dev
```

### 步骤 4：验证前端启动

启动成功后，在控制台会看到：

```
VITE v8.x.x  ready in xxx ms

➜  Local:   http://localhost:3000/
```

---

## ✅ 访问测试

### 1. 登录系统

访问：http://localhost:3000/

| 角色   | 用户名   | 密码       | 权限               |
| ------ | -------- | ---------- | ------------------ |
| 管理员 | admin    | admin123   | 全部功能           |
| 快递员 | courier1 | courier123 | 快递管理、取件管理 |
| 业主   | owner1   | owner123   | 仅查看数据统计     |

### 2. 测试取件功能

1. 使用管理员/快递员账号登录
2. 点击左侧菜单 **取件管理**
3. 测试两种取件方式：
   - 凭取件码取件
   - 管理员核验取件

### 3. 验证权限控制

- 使用 **业主** 账号登录，验证无法访问取件管理
- 尝试直接访问 http://localhost:3000/pickups，验证权限拦截

---

## 🔍 常见问题排查

### 问题 1：后端启动失败 - 数据库连接错误

**错误信息：** `Communications link failure`

**解决方案：**

1. 检查 MySQL 服务是否启动
2. 确认 `application.yml` 中的数据库配置正确
3. 确认数据库 `express_db` 已创建

### 问题 2：后端启动失败 - JAVA_HOME 未设置

**错误信息：** `Error: JAVA_HOME not found in your environment`

**解决方案：**

```bash
# PowerShell
$env:JAVA_HOME='D:\JDK'

# 或者设置系统环境变量：
# 右键「此电脑」→ 属性 → 高级系统设置 → 环境变量 → 系统变量
# 新建 → 变量名：JAVA_HOME，变量值：D:\JDK
```

### 问题 3：前端启动失败 - 依赖未安装

**错误信息：** `Cannot find module 'xxx'`

**解决方案：**

```bash
# 删除旧依赖并重新安装
rm -rf node_modules
rm package-lock.json
npm install
```

### 问题 4：前端无法调用后端 API

**错误信息：** `Network Error` 或 CORS 错误

**解决方案：**

1. 确认后端服务已启动（http://localhost:8080）
2. 检查 `.env.development` 中的 API 地址正确
3. 浏览器控制台查看具体错误信息

### 问题 5：端口被占用

**后端端口 8080 被占用：**

- 修改 `application.yml`：
  ```yaml
  server:
    port: 8081 # 改为其他端口
  ```

**前端端口 3000 被占用：**

- 修改 `vite.config.js`：
  ```javascript
  server: {
    port: 3001; // 改为其他端口
  }
  ```

---

## 📝 开发建议

### 1. 同时启动前后端

建议打开两个终端窗口：

**终端 1（后端）：**

```bash
cd express_system
$env:JAVA_HOME='D:\JDK'
.\mvnw.cmd spring-boot:run
```

**终端 2（前端）：**

```bash
cd express-frontend
npm run dev
```

### 2. 热重载

- 前端：Vite 支持热重载，修改代码自动刷新
- 后端：Spring Boot DevTools 已配置，修改 Java 文件自动重启

### 3. 日志查看

- 后端日志：控制台实时输出
- 前端日志：浏览器开发者工具 → Console

---

## 🛑 关闭服务步骤

### 方式 1：关闭终端窗口（简单直接）

- 直接关闭运行后端和前端服务的 CMD/PowerShell 窗口即可

### 方式 2：使用命令终止进程（推荐用于远程或无窗口）

```batch
:: 查看端口占用情况，获取进程 PID
netstat -ano | findstr ":3000"   # 前端
netstat -ano | findstr ":8080"   # 后端

:: 根据 PID 强制终止进程
:: 例如 PID 为 42416 和 43656：
taskkill /F /PID 42416
taskkill /F /PID 43656
```

### 方式 3：一键关闭所有相关服务脚本

创建 `停止项目.bat` 文件：

```batch
@echo off
echo ========================================
echo 快递管理系统 - 停止服务脚本
echo ========================================

echo.
echo [1/2] 停止前端服务...
for /f "tokens=5" %%a in ('netstat -ano ^| findstr ":3000" ^| findstr "LISTENING"') do (
    taskkill /F /PID %%a
    echo 已停止前端服务 (PID: %%a)
)

echo.
echo [2/2] 停止后端服务...
for /f "tokens=5" %%a in ('netstat -ano ^| findstr ":8080" ^| findstr "LISTENING"') do (
    taskkill /F /PID %%a
    echo 已停止后端服务 (PID: %%a)
)

echo.
echo ========================================
echo 服务已全部停止！
echo ========================================
pause
```

---

## 🎯 快速启动脚本（Windows）

创建 `启动项目.bat` 文件：

```batch
@echo off
echo ========================================
echo 快递管理系统 - 快速启动脚本
echo ========================================

echo.
echo [1/2] 启动后端服务...
start cmd /k "cd express_system && set JAVA_HOME=D:\JDK && .\mvnw.cmd spring-boot:run"

timeout /t 10 /nobreak

echo.
echo [2/2] 启动前端服务...
start cmd /k "cd express-frontend && npm run dev"

echo.
echo ========================================
echo 启动完成！
echo 前端地址：http://localhost:3000
echo 后端地址：http://localhost:8080
echo ========================================
pause
```

---

## 📞 技术支持

如遇到其他问题，请检查：

1. 各服务端口是否正常监听
2. 防火墙是否阻止连接
3. 数据库表结构是否正确创建
4. 前后端版本是否兼容

---

**最后更新：** 2026-05-19
